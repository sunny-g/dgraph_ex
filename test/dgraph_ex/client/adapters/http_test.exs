defmodule DgraphEx.Client.Adapters.HTTP.UnitTest do
  @moduledoc """
  Tests the HTTP client's RESTful implementation details against a mocked
  HTTPoison module (`TestHelpers.RequestMock`)
  """
end
